"use client";
export default function DiagramModal({open,conceptId,onClose}:{open:boolean;conceptId?:string;onClose:()=>void}){ if(!open) return null; return(<div className='fixed inset-0 z-50 bg-black/40 flex items-center justify-center'><div className='bg-white w-[min(100%,1000px)] rounded-xl shadow-lg overflow-hidden'><div className='px-4 py-3 border-b flex items-center justify-between'><div className='font-semibold'>SNOMED Diagram — {conceptId}</div><button className='border rounded px-3 py-1' onClick={onClose}>Close</button></div><div className='p-3 overflow-auto max-h-[80vh]'>{conceptId?<object type='image/svg+xml' data={`/api/snomed/diagram?id=${encodeURIComponent(conceptId||'')}`} className='w-full h-[75vh] border rounded bg-white'/>:<div className='text-sm text-gray-500'>No concept</div>}</div></div></div>); }
